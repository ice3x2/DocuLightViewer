# S20 startup recovery evidence

Requirements: `FR-DOC-019` (stable, in progress), `REL-DOC-009` (evolving, planned), `IR-APP-013` (evolving, planned), and `IR-APP-010` (stable, verified). Active target: `0.11.0-w2`. Starting commit: `12d176cbcd84e56aba820a13fbec43ac9dedbd26`.

## Change and RED evidence

`test/r3/cases/s20.cjs` uses actual Markdown files and SQLite databases in the prepared Node ABI 137 snapshot. The first RED run exited 1 at `second live controller cannot open the same ledger` (one assertion), proving that two controllers could open the same writable database. After the exclusive lock was implemented, a second RED run exited 1 at `publication gate recovers recycled PID without losing Markdown` (five assertions). After the publication identity fix, a SharedArrayBuffer barrier held the first recovery page; RED exited 1 at `owner publishes ready before first recovery page is unblocked` (six assertions). After moving the first page past START-ready, a worker fixture prohibited full ingress `readdirSync`; RED exited 1 at `private ingress replay accepts a saved intent without full directory materialization` (eleven assertions). Each RED was an assertion failure, not an ABI or fixture setup failure.

The owner lock is an atomically linked file beside the ledger. Its record contains PID, process-start identity, and a unique controller token. A second live controller is rejected before worker construction and SQLite open. An actual child owner process was exited without shutdown; a new owner recovered its dead lock. A fixture with the current PID and a different start identity proved safe PID-reuse recovery. On platforms where process-start identity cannot be established, a live PID is treated as unknown and the lock remains busy; dead PID proof still permits recovery. The publication gate uses the same liveness proof. No process was terminated by the implementation.

Independent review found a compare-and-rename race in that first lock version. A deterministic test replaced the dead owner record with a live A lock between B's stale read and rename; RED exited 1 at `serialized recovery rejects A between B stale-read and rename, then keeps C out`. The publication version of the same test also exited 1 with its sidecar disabled. Both lock paths now serialize *every* acquisition and stale recovery through an atomic `.recovery` directory. A cannot acquire while B inspects and renames. A process crash while holding that short sidecar gate fails closed and needs explicit operator inspection; no age-based deletion is used.

The worker sends cached keyword health and START-ready before the first interrupted-job page. The barrier test proves `start()` resolves while that page is held. Recovery uses the existing 32-job keyset page and yields between pages. The test populated 3,101 real Markdown files and SQLite `indexing` jobs; START-ready took 425–613 ms in sampled runs, cached status and cancel returned during recovery, and all 3,101 jobs left `indexing` within the test deadline. Private intent replay now streams directory entries and the per-intent lookup retains only same-document candidates. No startup path loads every ingress filename into an array.

Independent review also found that S19 legacy migration used an unbounded `.all()` and ran before START-ready. A fixture with 3,101 actual legacy jobs produced RED at `owner START-ready precedes first real legacy migration page`. Legacy migration now reads at most 32 jobs per keyset page after START-ready. The test records each page, observes query and cancel replies before migration completes, and verifies all 3,101 legacy jobs migrate. New save acceptance stays closed until migration commits; a Markdown file published during migration remains on disk, returns `enqueue_failed`, and its durable intent replays after migration. Unknown migration schema markers still fail before ready. S11/S19 assertions now wait for the cached `recoveryComplete` status because START-ready intentionally precedes reconciliation.

Existing `SearchEngine.initialize` and `ensureFresh` keep missing/root/tokenizer-incompatible keyword caches stale without an implicit rebuild; the lifecycle and worker tests cover the narrow explicit Settings rebuild, cancelled/failed rebuild preservation, and search compatibility. S09, S10, S11, and S17 cover ACK/replay idempotence, same-document revisions, publication ordering, and durable desired-job behavior. S10's post-START check now waits for asynchronous recovery; when a newer save is committed before recovery, the superseded old job may correctly be `cancelled` rather than `failed`.

## GREEN commands

Prepared snapshot: `C:/Users/beom/AppData/Local/Temp/doculight-r3-node-20260924d`; Node ABI 137; Windows. Source hash at the recorded S20 run: `36ca3047485ec1790a914d227e0b764d804f6d77b4dfb51384885481a9877501`.

| Command | Result |
| --- | --- |
| `node test/r3/run-node.cjs --case s20` | PASS, 21 assertions; 3,101 interrupted jobs and 3,101 legacy jobs; 32-job pages |
| `node test/r3/run-node.cjs --case s10` | PASS, 47 assertions |
| `node test/r3/run-node.cjs --case s11` | PASS, 25 assertions |
| `node test/r3/run-node.cjs --case s19` | PASS, 47 assertions |
| `node test/r3/run-node.cjs --case s09` | PASS, 37 assertions |
| `node test/r3/run-node.cjs --case s17` | PASS, 32 assertions |
| `node test/test-search-engine-lifecycle.js` | PASS |
| `node test/test-startup-index-recovery-memory-contract.js` | PASS, 8 MiB source bodies and zero retained content bytes |
| `node test/test-search-index-worker-contract.js` | PASS |

**Open issue boundary:** Product main still calls `SearchEngine.initialize()`, whose legacy startup reconciliation can use main-thread SQLite and `.all()` independently of this owner route. Product-wide DB-free startup therefore remains unproven and issue #43 must stay open. The S23/S26 product owner cutover must remove that path and prove the cold app route. Prior committed keyword cache preservation is covered by the existing lifecycle and worker tests, but a new S20-specific real SQLite checksum assertion remains to add before issue closure. No SRS status or verification field was changed. This evidence covers the safe owner startup slice and does not assert completion of the broader `IR-APP-013` UI state matrix or all `REL-DOC-009` release fault gates.

## Closure evidence after the S23/S26 product cutover

The preceding paragraph records the partial 8a098e checkpoint. At current HEAD `1b17ed75ac3916cf68ea3805e5f1da206f9ae631`, `SearchEngine.initialize()` is still called by product main to load the last committed index for public search, but owner-managed mode opens keyword SQLite read-only and skips its legacy startup reconciliation. The separate owner worker owns ledger and keyword writes. The real Electron S26 scenario instruments both SQLite store classes in the app main process, with no instrumentation in the owner worker. Its first new assertion failed because the private audit probe did not exist; that was a fixture failure, **not** semantic RED for product behavior. After adding the probe, an independent review found that writable-only counting missed a read-only ledger open on every Settings status request. The strengthened test counts all main store `open()` calls and reached semantic RED at source hash `822337a38bb32ab9b768e2c3c53e22b17529fa9cbe7b9a1ddae1557733179505`: `ASSERTION_FAIL case=s26 assertions=22 S20 real Settings status uses cached state without main SQLite open or query`. Product Settings now calls `getStatus({cachedOnly:true})`; this retains the status field shape while avoiding `getHnswCompactionStatus()` and its ledger read. The product has no registered semantic model, so `hnswCompaction` remains explicitly `null`. The final real Electron case proves zero writable main opens after cold startup, owner publication and public search, viewer focus/close, Settings status, and restart; it separately proves that second-instance focus, close_viewer, and Settings status each cause zero main SQLite store opens. Package-smoke's independent in-memory native probe is outside this app lifecycle.

An independent final review found a second cached-status path: with a legacy `IndexingWorkerController.activeJob`, `SearchEngine.getStatus({cachedOnly:true})` still called controller `getStatus()` and read the durable job from SQLite. `test/test-settings-indexing-contract.js` set an active job and instrumented the ledger provider; semantic RED exited 1 at `cached Settings status preserves the active legacy snapshot without a ledger open or query`. The controller now accepts `cachedOnly` and returns the same public in-memory status without `_readDurableJob`; its default status call still refreshes durable state. The final real Electron JSON records main SQLite open counters: Settings status 60 before/after, second-instance focus 43 before/after, close_viewer 53 before/after, and zero writable opens in each snapshot. This audit observes both store classes' `open()` calls in the main process; the focused active-job test also counts the ledger query.

`FR-DOC-022` AC-3 remains verified/stable and describes an HNSW compaction recommendation when an active model has tombstone or stale membership above its threshold. It is not formally superseded. `FR-APP-013` is implemented/evolving and requires semantic search to stay disabled after removing model registration. Product startup deletes legacy `semanticSearch` settings before constructing `SearchEngine` with no semantic config/provider (`src/main/index.js`); `_getSemanticSearchConfig().modelFingerprint` is therefore null. The previous `getHnswCompactionStatus()` returned null before selecting any committed ANN index, even when old ANN rows survived on disk. A direct product-options probe observed `modelFingerprint=null`, zero ANN lookups, and null for both legacy and cached status. S24's retained-ANN/legacy-settings test passed 29 assertions after the IPC test parser was made LF/CRLF tolerant. This proves the cached Settings null preserves currently reachable product behavior; a future feature that restores model registration would need owner-published compaction status for a DB-free Settings path. The generic non-cached `SearchEngine.getStatus()` path remains unchanged for explicit model fixtures.

The real S23 SQLite case already supplies the S20 committed-cache preservation proof. It records generation ID, logical SHA-256 over committed document and segment rows, and a body-only search hit before and after a forced pre-commit failure. The generation and checksum remained `generation-929ab7de-ad6f-40de-a391-77170a086f2a` and `0eaae7558af68a92f4228c8590dd83f160e26be882d8f812b60a23ed693edaa4`. A separate mid-rebuild cancellation preserved generation `generation-21353a77-02ad-48b3-976b-536ad1afe472`, checksum `8a291ef5416513468ed2e4e25dbca4ce9653a706498e8379a2e650d849008f86`, and the prior body-only hit. The same case confirms missing index stays stale with no automatic keyword rebuild and only interrupted explicit Settings rebuild restarts. Its owner query remains available during staged maintenance. S26 independently demonstrates real Electron restart of a cancelled pending revision and eventual public search convergence.

An abandoned `.recovery` sidecar is a fail-closed ownership gate, not a lease. To recover it safely, first close every DocuLight app instance for the affected profile. Inspect the exact PIDs and full command lines of any remaining Electron or helper process for that profile, along with the adjacent `.owner.lock` or `.publication.lock` record. If any process may still own or acquire the lock, leave the sidecar intact. Once the profile is fully stopped and no contender remains, remove only the empty `<lock-path>.recovery` directory using its verified absolute path, then restart DocuLight. Do not remove the lock file, ledger, keyword database, ingress intents, saved Markdown, or a sidecar merely because it is old. If process identity or ownership cannot be established, keep the retryable `owner_busy` diagnostic and defer removal until the profile can be stopped and inspected. S20 asserts both an abandoned sidecar and unsupported live-PID identity reject acquisition without changing the lock; a recycled PID with a proven different start identity is automatically recovered.

Focused validation on Windows x64, Node 24.16.0 ABI 137, Electron 33.4.11 ABI 130: final source hash `7038c8ed48b3c135427a992af1506e0cabd628149399439dc3f3abdaf6d6fdbf`; S20 `PASS 23` (3,101 modern plus 3,101 legacy jobs, maximum 32 per page, 411 ms START-ready); real Electron S26 `PASS 49`; real Electron S21 `PASS 23`; S24 legacy embedding and LF/CRLF IPC probe `PASS 29`; Settings indexing, index-worker, status performance, and Settings status poller contracts passed. S23 `PASS 45`, S19 `PASS 47`, S17 `PASS 32`, `test-search-engine-lifecycle`, and `test-startup-index-recovery-memory-contract` passed before the final controller cached-status change, which leaves their default `getStatus()` path unchanged. The final S26 run's generated raw JSON, including inspected product PIDs 55480 and 65160 and the counter snapshots, is preserved at `docs/analysis/2026-09-25-s20-s26-7038c8ed48b3c135427a992af1506e0cabd628149399439dc3f3abdaf6d6fdbf-raw.json` (file SHA-256 `ec58399cc0e7975f33f75993c2827370a030ecb3d7e317ce0f0fa802e9ec8323`). The original S20 fixed 200 ms readiness samples could fail on a loaded host; both barrier checks now wait for READY for up to five seconds while still failing if READY waits behind blocked recovery. Independent re-review is separate. This S20 evidence does not mark the broader REL-DOC-009 or IR-APP-013 requirements verified.
