# S09 owner acceptance evidence

Issue [#32](https://github.com/ice3x2/DocuLightViewer/issues/32) starts at `11be24519754e14d6e4af1b9f4e22c300476ac86` on `feature/issue-24-s01-sol-medium`. Active target `0.11.0-w2` has no draft/deprecated stability blocker. Relevant requirements: `FR-DOC-019`, `REL-DOC-009`, `DR-DOC-014`, `IR-MCP-018`, and `IR-APP-013`.

## RED and GREEN

The new real-filesystem and real-SQLite `test/r3/cases/s09.cjs` was registered before implementation. After refreshing the existing source-only Node/Electron snapshots, `node test/r3/run-node.cjs --case s09` exited **1** with `ASSERTION_FAIL case=s09 assertions=1 owner ACK follows durable job and metadata transaction`. The old owner returned `owner_not_implemented`; the assertion specifically required a durable queued ACK. The later same-path revision test exited **1** with `ASSERTION_FAIL case=s09 assertions=19 only latest pending job stays queued`. The existing source/display and document metadata preservation test exited **1** with `ASSERTION_FAIL case=s09 assertions=10 existing source and derived document metadata are preserved` (split assertions isolated source display metadata as the remaining failure).

The final independent review found a same-millisecond publication ambiguity. A valid private two-intent fixture forced equal `createdTime` values while keeping distinct aliases and metadata; the observed RED was **exit 1**, `ASSERTION_FAIL case=s09 assertions=34 equal timestamp cannot make older same-body intent current`. S09 now fails closed for both contenders with a retryable warning and no queued/job ID. Both private intents and final Markdown remain. This is a transitional limit: S10 must add a durable publication-order witness or equivalent owner-authoritative tie resolution so equal-millisecond saves converge without requiring another user save.

Independent review found receipt and recovery gaps. The repair assertions were authored before the repair code. The observed repair REDs were **exit 1**, `ASSERTION_FAIL case=s09 assertions=7 provenance-only older intent never borrows the current job receipt`, and **exit 1**, `ASSERTION_FAIL case=s09 assertions=26 same-hash older intent cannot become latest while a newer intent awaits acceptance`. The AC-8 field and cleanup assertions were already present in this RED suite but could not execute until the earlier failing assertions passed; no separate observed RED is claimed for them. A further crash-window test produced **exit 1**, `ASSERTION_FAIL case=s09 assertions=29 crash-window fixture leaves committed intents for receipt replay` before the replay cleanup branch was added.

For a bounded corrective mutation check after the first review, the author temporarily changed only the new `dirty = 1` SQL assignment to `dirty = 0`. The focused case then exited **1** with `ASSERTION_FAIL case=s09 assertions=19 initial acceptance atomically records desired hash, requested revision and dirty state`. The assignment was restored immediately before the final GREEN run. This is a later corrective RED, not a claim that the original AC-8 assertion executed in the first RED run.

Final source hash `84ad439c7b2c2b577b69169da8ffa76e106cbfa6ed1e0b8305127d6fa5cb6a69`. `node test/r3/prepare-runtime.cjs --node-root %TEMP%/doculight-r3-node-20260924d --electron-root %TEMP%/doculight-r3-electron-20260924d` refreshed source snapshots without dependency install or native rebuild (Node ABI 137, Electron ABI 130). With `DOCULIGHT_R3_NODE_ROOT` set to that Node root, `node test/r3/run-node.cjs --case s09` exited **0** in about **1.05 seconds** with `PASS case=s09 assertions=37`.

## Fault and replay matrix

| Fixture | Observed result |
|---|---|
| Two same-path saves before owner accept, with distinct original aliases and metadata | New final Markdown bytes and hash retained; both lexical/canonical aliases link to one stable document ID; old category/tag/description and new title/tag plus existing source/document metadata survive. The older provenance-only receipt has no job ID or desired revision. |
| SQLite trigger aborts `index_jobs` insert | Owner returns `saved=true`, `accepted=false`, `indexing.state=enqueue_failed`, no `jobId`; final file and both private intents remain; document metadata/revision and job row roll back. |
| External SQLite `BEGIN IMMEDIATE` lock | Owner returns the same retryable saved-file failure envelope, with no false queued ACK. |
| Same `intentId` replay and owner restart | An accepted intent returns its own original desired revision and job ID after a newer acceptance; persisted metadata, tags, aliases, current revision, and job ID are byte-equivalent after replay. An unaccepted older intent returns no false queued ACK when a newer file is published. |
| Owner unavailable after shutdown | Private controller adapter returns saved-file enqueue failure without a job ID. |
| New same-path intent after an accepted save | Desired revision rises from 1 to 2, final file hash becomes current, and only the latest job remains queued; the previous queued job is cancelled in the same transaction. |
| Same final body with distinct intents | Both pending aliases and metadata are committed before the newer intent's queued ACK. An older same-hash intent cannot become current after a newer intent is published. |
| Same millisecond timestamp on distinct same-body intents | Both contenders return retryable no-ACK/no-job; both private intents and final Markdown remain. S10 must resolve publication order and convergence without requiring a user re-save. |
| Transient read failure on older pending intent | Current ACK fails retryably without job ID; the older private intent remains for provenance recovery. |
| Crash after ledger commit, before private intent cleanup | Receipt replay returns the same job ID, then removes the committed private intent. Provenance-only older receipt cleanup likewise removes its private file without touching final Markdown. |
| Authoritative pending fields | `desired_content_hash`, `active_requested_revision`, `dirty`, `keyword_dirty`, and `accepted_intent_id` are committed with revision, metadata and job. |

Adjacent regression commands also passed: `run-node.cjs --case s08` (53 assertions), `run-node.cjs --case s06` (42 assertions), `node test/test-wave2-mcp-contract.js`, and `node test/test-wave2-ledger-contract.js`; `git diff --check` exited 0. A UTF-8 JSON key comparison confirmed identical **254** locale keys in `ko`, `en`, `ja`, and `es`. No locale or public MCP schema was changed. The existing public `save_document` producer still uses the old path until #37; this evidence covers the private owner result and saved-file failure mapping only. S10 retains the complete dirty/latest-winner indexing lifecycle. Committed private intents are removed only after their per-intent durable receipt is visible; uncommitted intents remain for retry.

Independent [ledger/recovery review](2026-09-24-s09-data-review.json) and [TDD/public-contract review](2026-09-24-s09-tdd-review.json) passed with no remaining findings. The same-millisecond ordering ambiguity is intentionally fail-closed in S09 and must converge automatically under S10 #33; the S09 result is not product-wide completion. Original pre-implementation RED transcripts/source snapshots were not archived, so exact historical chronology is not independently reconstructable; the later corrective AC-8 assertion RED was recorded separately and is not presented as the original RED.
