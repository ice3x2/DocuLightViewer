# S16 original provenance and external save-as evidence (review pending)

Base: `0bc5d11b404d0499ebd4e0daefc244c1f18dfada` in isolated `DocuLightViewer-r3`. Active target: `0.11.0-w2`, with no Stability blockers. Relevant requirements: `FR-DOC-035` implemented/evolving, `DR-DOC-014` in_progress/evolving, `FR-DOC-036` in_progress/evolving, `FR-DOC-019` in_progress/stable, `REL-DOC-009` planned/evolving, and `SEC-DOC-003` verified/stable.

The source-backed registrar now captures the lexical path before reading the canonical target. It resolves and checks the canonical path after reading, then passes the chosen lexical path, canonical path, canonical path hash, and content bytes into the body-free S08 intent. The contained `.opened` copy is published atomically before the S09 owner accepts it. The owner transaction records the original aliases, stable document identity, revision, and job. A second lexical alias with unchanged bytes is accepted as `provenance_only`, adding an alias without a duplicate indexing revision. Content-only saves still carry no invented aliases. The external renderer save-as and subsequent quick-save retain the chosen file and existing `{success,filePath}` IPC response; they publish a separate contained copy and no longer call the external legacy `markDirty` path. Acceptance failure leaves both files and the private intent for retry.

The original opened-markdown registrar and indexed-origin contract assertions remain in place. Their standalone fixtures now start a real owner so external registration exercises the required single-writer boundary. The indexed-origin contract continues to cover read-only origin selection, hash-only fallback, mismatch, and public redaction. External `.markdown` origins retain their baseline `.markdown` contained locator. S08 now permits that extension only for source-backed `opened_markdown` or `update` intents with original aliases, so a pre-S16 copy reopens and updates under its existing document ID. A copy changed outside the owner is protected by the ledger hash at the registrar and atomic publisher; it is not overwritten. A pre-publication destination failure leaves a bounded, body-free private `.registration.json` marker with the origin identity, file hash, root fingerprint, retryable flag, and redacted diagnostic code. The marker is retained for a later user-triggered retry; it is not an automatic replay job.

## TDD and checks

- Semantic RED: `node test/r3/run-node.cjs --case s16`, exit 1 at `external open uses durable owner and receives stable document identity` (0 assertions passed). Fixture and Node ABI snapshot were valid.
- GREEN: same command, exit 0, 23 assertions. The real owner case covers a junction lexical alias sharing a canonical target and document ID, separate contained bytes, alias-only acceptance without a new revision, changed content with the same identity, hash-only null fields and verified backfill, external save-as and quick-save, owner failure with retained external file/copy/intent, explicit retry, and fresh-owner restart.
- Additional semantic RED: external `.markdown` publication failed at assertion 1 because S08 accepted only `.md` copies; a seeded pre-S16 `.markdown` copy/alias failed reopening at assertion 1 after the temporary filename workaround, proving that migration would break stable ID. The final repair retains the baseline locator and scopes S08 extension acceptance. Divergent contained-copy protection failed at assertion 2; publisher's expected-prior-hash check failed at assertion 3; and a pre-publication destination blocker failed at assertion 14 because no private diagnostic was retained. Each assertion passed after the scoped repair.
- Adjacent Node ABI 137 checks: S08 exit 0 (53 assertions), S14 exit 0 (20 assertions), S15 exit 0 (26 assertions), `test-opened-markdown-registrar-contract`, `test-indexed-origin-open-contract`, `test-mcp-tool-parity`, and `test-mcp-http-save-parity` exit 0. A concurrent S14 run failed transiently during an owner ACK; a scoped standalone rerun passed. The generated ignored MCP bundle was copied into the Node snapshot for the indexed-origin test.
- Runtime roots: `C:/Users/beom/AppData/Local/Temp/doculight-r3-node-20260924d` (Node ABI 137) and matching `doculight-r3-electron-20260924d` (Electron ABI 130). Source-only snapshot hash: `569dffbfd666b8b73b57cfa2670f75883e9c21dd652111ec30a5c60fa756f9f5`.
- `git diff --check` exit 0. Independent review is pending; this file does not assert that #38 or #39 is complete.

The remaining #38 completion evidence is actual source stdio and renderer manual IPC/window failure behavior, followed by independent review. #38 and #39 remain open at this checkpoint.
