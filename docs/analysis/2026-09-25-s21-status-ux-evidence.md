# S21 Settings and viewer indexing status evidence

Issue: [#44](https://github.com/ice3x2/DocuLightViewer/issues/44). Start SHA: `8a098e3d9fdfa22abff84b67e157e05130ec3a8c`. Isolated worktree: `C:/Work/git/_Snoworca/DocuLightViewer-r3`. Requirements: `IR-APP-013` (planned/evolving), `IR-APP-010` (verified/stable), `FR-APP-006` and `FR-APP-007` (implemented/stable), `FR-DOC-019` (in_progress/stable), `REL-DOC-009` (planned/evolving). Active target: `0.11.0-w2`; no stability blockers.

## RED and GREEN

The S21 Electron scenario uses the actual Settings and viewer HTML/renderer scripts, Electron preload, and private `ipcRenderer.invoke` handlers. The first run after adding `test/r3/cases/s21.cjs` and registering it in both Electron runners returned exit 1 with `ASSERTION_FAIL case=s21 assertions=0 Settings ledger live region has required ARIA contract`. This was an assertion failure after Electron setup, not a module or ABI failure. A second assertion RED covered saved-success plus capacity-deferred viewer feedback. The separate 30-state registry test first failed on the exact state list assertion with an empty stub registry.

After implementation and snapshot refresh, `DOCULIGHT_R3_ELECTRON_ROOT=C:/Users/beom/AppData/Local/Temp/doculight-r3-electron-20260924d node test/r3/run-electron.cjs --scenario s21` returned exit 0, `PASS case=s21 assertions=23`. The scenario covers the Settings live-region attributes; queued, indexing, ready, capacity-deferred, retryable and blocked-owner copy/actions; progress bucket and heartbeat announcement coalescing; terminal 100%; recovery alert; actual SearchEngine legacy cancel/retry IPC behavior and focus; active legacy rebuild announcement over parallel owner READY with Cancel disabled; and viewer saved-success versus queued/deferred indexing with the original saved path. The native Electron ABI is 130 on Windows and the reviewed snapshot source hash is `324e8edff76aae5c0a25182f62da7f43bdaa1f7cdf5059d9ca5fd4836788078b`.

Focused regressions passed: `node test/test-ledger-status-registry.js` (explicit 30 canonical state/code pairs and all four locale label sets), `node test/test-settings-indexing-contract.js`, `node test/test-settings-status-poller-contract.js`, `node test/test-status-coalescer-contract.js`, `node test/test-settings-linked-import-uncertainty.js`, and `node test/test-wave2-mcp-contract.js`. R3 Node cases `s18` and `s20` passed with 19 and 21 assertions respectively. The legacy public `state` remains in its prior precedence path, while private `ledgerState`, `ledgerCode`, `ledgerPhase`, `ledgerProgress`, and `ledgerCondition` are added to Settings IPC from `saveDocumentOwner.getStatus()` cached memory. No MCP tool or envelope was added.

Independent review of the first S21 hash found that canonical cancel/retry buttons called legacy SearchEngine handlers and could report success for the wrong job. A revised Electron assertion first failed at `KEYWORD_REPAIRING` because the unsupported cancel button was enabled (exit 1, assertion 2). Canonical owner cancel and retry are now disabled in Settings until a real owner action is wired. The Electron case proves that clicking either disabled button does not invoke the legacy IPC handler, and that a legacy `started:false` retry result remains visible after the promise settles. The review also found that global Settings capacity copy claimed a file had been saved without a receipt. It now reports only deferred indexing; the viewer's own successful save receipt still says Saved and preserves its prior file path. The explicit 30-state code oracle and four-locale parity test pass.

A further review found the first owner-action gate suppressed legacy controls whenever the parallel cached owner state was present. A product-shaped payload with public `state=rebuilding` plus private owner `READY` exposed the conflict. The product `getIndexingStatusPayload()` now uses `composeIndexingStatusPayload()`, which the Electron test also calls to construct mixed status. The final control gate preserves baseline full-rebuild cancellation safety, permits supported non-rebuild legacy worker cancellation, and permits Retry when legacy failed-file count is positive with no legacy operation active. Owner-only CHECKING/KEYWORD_REPAIRING/ANN_BUILDING/CORRUPT_DEGRADED/INTERRUPTED cannot call those legacy handlers. The test invokes the real `SearchEngine.prototype.cancelRebuild` and `retryFailures` methods through private IPC and checks responses and retained diagnostics. This preserves public-state precedence without claiming owner controls are implemented.

Another review found the mixed payload still displayed the owner READY label during an active legacy rebuild. A new Electron assertion failed at `active legacy rebuild is announced ahead of parallel owner READY` (exit 1, assertion 13). The renderer now uses localized active legacy labels in the existing `role=status` live region when an actual legacy rebuild session, worker, or native repair is active; owner-only states retain their canonical label and progress. A concurrent capacity condition remains in the announcement without claiming a document save. All four locale files carry the same legacy label keys, and the Electron scenario passed 22 assertions after this change.

The next review found that permitting Cancel for an active full legacy rebuild contradicted the baseline Settings safety guard. An Electron assertion failed at `full legacy rebuild keeps the baseline safe-cancel guard` (exit 1, assertion 15) for the same mixed public rebuilding/owner READY payload. Settings now requires a real active **non-rebuild** legacy worker and excludes full rebuild and native repair. The Electron scenario proves full rebuild displays “rebuilding” but disables Cancel and sends zero private IPC calls; a separate non-rebuild worker still uses the actual `SearchEngine.cancelRebuild()` path with focus preserved. The Settings contract check again asserts the full-rebuild guard. S21 passed 23 assertions after the correction.

## Dependency boundary

S20 #43 is still open after its partial commit. This S21 change does not assert product-wide main-process DB-free status/focus/close: `getIndexingStatusPayload()` still combines the existing `searchEngine.getStatus()` legacy path with the cached owner snapshot, and `SearchEngine.initialize()` still schedules legacy SQLite reconciliation until S23 #46 and S26 #49 cut over. The S20 owner currently emits a subset of the approved 30 canonical states; the registry and four-locale copy accept all 30 for later owner transitions. Main-process dispatch of the complete P0 action matrix and release latency evidence remain outside this UI/locale slice. Ingress capacity before file publication can still fail the save producer; this UX displays a saved-success plus deferred result only when that private save result actually reports success. The S17/S19 publication boundary must supply that result for a real capacity fault. Embedding registration UI remains in the baseline and is assigned to S24 #47; this S21 slice must not be used to claim #44's full removal criterion or close #44.

The private S21 test fixture returns saved-success with `indexingState=enqueue_failed`, `warningCode=indexing_ingress_capacity`, and no jobId. It does not modify the public eight-tool MCP surface or expose a new ledger field there.
