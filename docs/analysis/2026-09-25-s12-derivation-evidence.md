# S12 document derivation evidence

Issue [#35](https://github.com/ice3x2/DocuLightViewer/issues/35). Requirements: FR-DOC-019, REL-DOC-004, DR-DOC-007, DR-DOC-008, DR-DOC-014. Start SHA: `d421a49730d635b75cf58c3eda4bbdccfc28605e`.

The private owner can derive a validated saved document when `deriveDocuments: true` is configured. S11's final file/hash/revision check and publication gate run before derivation. The ledger transaction updates classification, heading-aware chunks, links, and the revision guard together. A separate per-document keyword transaction replaces old FTS rows and segments; its failure leaves `keyword_dirty = 1` for retry, while accepted metadata, aliases, and the saved file remain. The private switch keeps S10 acceptance-only fixtures stable until producer wiring in the later transition issue. No public `index_document` command or automatic full rebuild was added.

RED: after adding `test/r3/cases/s12.cjs` and a module stub, `node test/r3/run-node.cjs --case s12` exited 1 with `ASSERTION_FAIL case=s12 assertions=0 validated document atomically derives metadata, chunks, links and keyword cache`. Independent review led to additional semantic REDs: an existing source-root mismatch exposed old-root rows after a new save (`assertions=16`); malformed later category/tags erased or polluted prior user metadata (`assertions=10`); a derivative cache revision ahead of the authoritative ledger was falsely treated clean (`assertions=19`); bracketed `[bad]` category/tag metadata still erased or polluted prior valid values (`assertions=11`); the structured-scalar guard rejected valid inline `documentTags: [alpha, beta]` (`assertions=13`); and malformed nested bracket/object inline tag lists indexed fragments (`assertions=14`, separately observed for each form). All were assertion failures with the prepared Node ABI 137 snapshot.

GREEN: `node test/r3/run-node.cjs --case s12` exited 0 with `PASS case=s12 assertions=28`. It covers mixed Korean/English Markdown, headings and fenced code, explicit category/tags, valid inline tag lists without separate accepted metadata, malformed nested bracket/object inline lists with no fragment tags, malformed metadata fallback and prior-value preservation, links, ledger commit rollback, keyword transaction rollback, retry and dirty clearing, stale revision rejection, old-only FTS token removal, another document's FTS retention, metadata/alias preservation, private owner background drain, incompatible source-root and tokenizer caches, and authoritative ledger replacement when a derivative revision is ahead. Incompatible committed caches remain stale with durable `keyword_dirty` until explicit rebuild; existing rows are not silently exposed or deleted. The exact old term is checked with an FTS phrase query because the compatibility tokenizer intentionally ORs n-grams for `keyword.search`.

Focused regressions: S11 24, S10 46, S09 37, S08 53, S06 42 assertions passed. `node test/test-sqlite-keyword-index-contract.js` and `node test/test-wave2-ledger-contract.js` passed. `git diff --check` exited 0. Windows x64, Node v24.16.0 / ABI 137, Electron 33.4.11 / ABI 130; existing R3 snapshots were refreshed without native rebuild. Windows resource exhaustion prevented simultaneous native test processes, so regressions were rerun sequentially and passed. Frozen source-only hash: `c231ea047724ae28477b31496b266b4d5474e8166747b683fb2aa6aeaac7e368`.

Independent review is pending. The owner pipeline remains private and opt-in until the later producer transition connects it to product save paths.
