# S23 product-main cutover: safe partial evidence

- Start: `e6c905a957c8bf9967d595efce4ea39f841be7b6` in isolated `DocuLightViewer-r3`.
- Requirements: `FR-DOC-019` (in_progress/stable), `FR-DOC-033` (in_progress/stable), `FR-DOC-035` (implemented/evolving), `DR-DOC-014` (in_progress/evolving), `REL-DOC-009` (planned/evolving). Active target `0.11.0-w2`; no draft/deprecated blocker.
- This is a **partial S23 result**. Do not close #46, #43, or #45 on this evidence.

## Exact target inventory and call graph

`git ls-tree -r --name-only e6c905a9 src/main` contains `search-index-worker-controller.js`, `search-index-worker.js`, `search-owner-controller.js`, `search-owner-worker.js`, `search-engine.js`, `search-sqlite-store.js`, `source-ledger-store.js`, and `index-ingress-store.js`. It contains no `candidate-saga-owner.js`, `candidate-io-worker.js`, `indexing-worker-runtime.js`, graph coordinator, or cross-worker root-grant module. Those private candidates are absent/no-op; there is nothing to delete.

| Candidate/site | Caller and substitute | Legacy data and decision |
| --- | --- | --- |
| Product main keyword `SQLiteKeywordIndex.open()` | `index.js` → `SearchEngine.initialize()` → `_loadIndex()`; main uses a read-only handle while `search-owner-worker.js` remains the writable owner | Keep committed index, metadata/source-root/tokenizer checks, and filtered public search; change main open mode only. |
| Product main startup ledger reconciliation | `SearchEngine.initialize()` previously scheduled `reconcileStartupIndexJobsAsync()` → writable `_getAvailableSourceLedger()`; owner `START` schedules `resumeLegacyMigration()` then bounded recovery | Do not schedule legacy reconciliation in owner-managed product mode. Preserve the old adapter in `SearchEngine` for non-owner callers and S19 data readers. |
| Product main active status ledger | `getIndexingStatusPayload()` → `SearchEngine.getStatus()` → `IndexingWorkerController.getStatus()` → `_readDurableJob()` → `_getSourceLedger()` | Active status now uses a temporary read-only ledger handle in owner-managed mode. Existing legacy controller mutation calls remain live. |
| Owner SQLite writable opens | `index.js` → `getSaveDocumentOwner()` → `OwnerWorkerController.start()` → `search-owner-worker.js` → `SourceLedgerStore.initialize()` and `SQLiteKeywordIndex.open()` | Keep: sole-owner lock, S19 migration marker, old journal transition, S17 import recovery and private ingress replay. |
| Old index worker/controller | `SearchEngine` constructor and Settings rebuild/cancel/retry/compact/clear, plus semantic work | Live. Its `ledgerProvider`, worker keyword writes, and main job patches prevent full sole-writer claim. Preserve until S26 replacement and compatibility evidence. |
| Linked import and opened registration | Settings linked-import IPC calls `searchEngine.getSourceLedger()` with `searchEngine.ownerController || null`; opened registrar and semantic registration also reach legacy ledger writes | Live. No safe deletion without an owner command substitute. |
| Rollback/cleanup candidates | `search-owner-worker.js` removes accepted private intents; `index-ingress-store.js` removes private temp/lock files. `SearchEngine` semantic reindex rollback updates job status | No reachable saved Markdown delete/before-image rollback found. Private cleanup and old job-status handling remain necessary; no deletion. |

The owner wire registry remains `accept_save`, `resolve_origin`, `query_keyword`, `get_status`, `cancel_job`, `shutdown`. Existing `query_keyword` has only a fixed simple query/limit path; it cannot replace public `search_documents`, `search_projects`, or `smart_search` filters. The product therefore keeps the read-only SearchEngine query facade.

## RED, GREEN, and remaining gate

`node test/r3/run-node.cjs --case s23` was registered and failed with `ASSERTION_FAIL ... product main opens keyword SQLite read-only` after a real committed keyword SQLite fixture was present. A second RED failed `active product status never opens a writable main-process ledger`. Independent review found that coupling `getSaveDocumentOwner()` success to `SearchEngine.initialize()` hid a prior committed index when the owner could not start. A third semantic RED extracted and ran the actual product `initializeSearchEngineIfConfigured()` function against real committed SQLite: `ASSERTION_FAIL ... owner startup failure still loads previous committed product search index`. The final product function starts owner recovery and the read-only search load independently. These were assertion failures, not ABI/setup failures. After the scoped changes, S23 passes 9 assertions at sourceHash `16fef874888cc5847419e32642a79ab610cdaed62e569bd3412b434a89f6016c`. The case injects a main-process SQLite constructor that rejects writable keyword opens, instruments actual source-ledger opens during active status, and confirms the previous committed keyword result remains searchable even when owner startup fails.

Using the prepared Node ABI 137 snapshot on Windows: S17 `32`, S19 `47`, S20 `21` assertions pass; S20 recovered `3,101` pending jobs in bounded pages. `test-search-engine-lifecycle.js` and `test-sqlite-keyword-index-contract.js` pass. The snapshot Electron ABI is 130. No S23 cold product Electron session was run.

S26 #49 must replace or confine the remaining live Settings/worker/linked-import/opened-registration/semantic ledger mutation routes, prove no product-main writable source/keyword SQLite open under real startup, search, status, cancel, focus, and close IPC, and preserve the committed index checksum after failure/cancel. S20 #43 also retains its sidecar recovery gate. S22 #45 retains real product IPC latency and focus/close evidence. S23 #46 cannot meet the sole-writer completion criterion until those paths are cut over.

## Post-S26 continuation from `414110745d77a9cb4a21e2e44e45b677ec05dfc3`

S26 #49 subsequently passed a real cold Electron two-PID save/import/cancel/restart lifecycle and closed. It also moved Settings linked import to `getSaveDocumentOwner()` plus `_openReadOnlySourceLedger()`. The earlier S23 handoff above is historical; its S26 dependency is resolved, while #46/#43/#45 retain their separate unfinished gates.

The current product indexed-origin `open_markdown` route remained `mcp-http.mjs`/`index.js` → `resolveIndexedMarkdownOpen()` → `searchEngine.getSourceLedger()` and opened the source DB writable in main merely to look up an existing identity. A new S23 test used a real seeded SQLite source DB, rejected any main writable `SourceLedgerStore.open()`, and failed with `ASSERTION_FAIL ... product indexed-origin lookup is read-only and preserves unknown-ID contract` (exit 1). The resolver now takes a temporary `_openReadOnlySourceLedger()` handle, reads its detached target, and closes it before origin/copy file validation. Existing non-product stubs retain their prior `getSourceLedger()` fallback. S23 passes 10 assertions; `test-indexed-origin-open-contract.js` passes including origin/copy fallback and public HTTP/stdio behavior. Source hash of this continuation is `b5548df835acfec2efa02be2db258d6079198a16408a122622e4e2b7ac41dbfd`.

Current focused regression results in prepared Windows x64 snapshots: Node 24.16.0/ABI 137 S20 `PASS21`, S25 `PASS16`, S23 `PASS10`, indexed-origin contract pass; Electron 33.4.11/ABI 130 real product S26 `PASS25`, both `S26_FIRST_RUN_COMPLETE` and `S26_RESTART_RECOVERED` reached. S19 migration and S17 partial import adapters were not removed. S26 confirms the product's existing eight-tool/search behavior on its exercised paths; this continuation changes only indexed-origin source lookup.

### Remaining actual writer routes and guarded SRS proposal

| Live route at 4141107 | Current caller/write | Owner substitute needed before retirement |
| --- | --- | --- |
| Settings `indexing:start-rebuild`, `retry-failures`, `compact`, `clear` | `index.js` → SearchEngine → short `IndexingWorkerController` with main writable `ledgerProvider`; `search-index-worker.js` opens keyword DB writable. Rebuild completion also invokes main `_resetSearchDerivedStateAfterFullRebuild()` and semantic reindex. | Bounded owner commands with durable job/status/cancel behavior and existing Settings response shapes, committed-generation preservation, native failure/retry evidence. Keep old worker until replacement passes. |
| Opt-in contained Markdown open/save-as registration | `opened-markdown-registrar.js` → `getSourceLedger()` → `createLegacyAdopter().adoptMarkdownFile()` → source/document/job writes and `queueKnownDocumentIndex()` in main. | Owner-side `adopt_existing` (or equivalent): verify contained existing file and hash without rewriting it, preserve ID/metadata/aliases, commit desired revision/job once, return no-op on unchanged content. External registration already publishes through `accept_save`. |
| Indexed-origin open | `resolveIndexedMarkdownOpen()` formerly `getSourceLedger()` writable in main. | **Done here:** temporary read-only query and close, same public result/error contract. |
| Semantic Settings callbacks | S24 removed the product embedding-registration UI/main IPC and credentials; `index.js` no longer calls `clearSemanticDerivedState()` or `queueSemanticReindexForActiveDocuments()`. | Do not delete shared methods yet: SearchEngine rebuild completion still reaches semantic reset/reindex through `_handleIndexingWorkerCompleted()`. Move that work with owner rebuild. |
| Search/status | `search_documents`, `search_projects`, `smart_search` retain owner-managed read-only SearchEngine with filters; active status has read-only ledger; owner `query_keyword` is only a simple fixed-limit query. | Preserve the read-only facade and public eight-tool contracts. Do not replace it with the limited owner query. |

At the `4141107` inventory point, `FR-DOC-019 AC-10` fixed the private registry to six commands. The Settings maintenance command was subsequently added as an unchecked SRS plan, as recorded below. Full #46 remains OPEN until the implementation, instrumented product-main/short-worker zero-writable-open test, S17/S19 migration, and failure/cancel committed-index checks are complete. #43/#45 retain their independent checksum/sidecar and actual product latency gates.

## Settings maintenance SRS plan and rejected implementation

From clean `0afbd15fa88f5025c1a04d46c2b8ba957994d3c2`, SpecKiwi CLI dry-run/edit added an unchecked private `manage_index` command to `FR-DOC-019 AC-10` with the strict `{operation}` enum `rebuild|retry|compact|clear`. The AC binds state admission to `IR-APP-013 AC-15/16`, preserves Settings result shapes and MCP boundaries, and distinguishes rebuild/compact failure from confirmed clear. Status and Stability were unchanged. `speckiwi validate --json` reports 0 errors and 8 pre-existing warnings. This SRS text is a **planned contract**, not evidence of implementation.

An experimental owner implementation passed focused S23 assertions but two independent reviewers placed it on **HOLD: unsafe to commit**. Their real SQLite review identified five blocking gaps: stale/missing-index `retry` was rejected despite legacy `retryFailures()` semantics; product main closed its read-only keyword handle before compact ACK, hiding a body-only committed hit during compact or rejected clear; direct `indexing:clear` IPC lacked a confirmation boundary; owner `VACUUM` and backup/delete/reopen could block query/cancel for seconds; and rebuild skipped the legacy scoped post-commit derived reset/requeue. An attempted use of `clearSearchDerivedState()` was also found to delete every `index_jobs` row, including unrelated jobs. Focused `PASS31` and S26 `PASS25` did not cover those failure modes and **must not be used as completion evidence**.

The experimental product/owner/controller/search/test changes were restored to clean `0afbd15` source. The existing live Settings short worker remains because removing it before a safe replacement would break controls. The opt-in contained registrar still writes its source ledger in main. No user Markdown, DB, S17/S19 recovery adapter, or public MCP contract was removed. The next implementation slice must first add failing product-level or owner-level assertions for all five findings, then use bounded owner maintenance with confirmed clear admission, durable idempotent jobs, old-generation visibility through failure/cancel, scoped derived reset, and cold Electron main/short-worker writable-open instrumentation. Keep #46, #43, #44, and #45 OPEN.

Post-rollback standard R3 sourceHash is `175381d337ea84c434b2aedac1144a12a9b6f9816a7d27fe635bec0c8fdc541c` (the unchanged product source plus planned SRS text). Prepared Windows x64 Node 24.16.0/ABI 137 checks: S23 `PASS10`, S20 `PASS21`, S25 `PASS16`, existing Settings indexing contract pass. Electron 33.4.11/ABI 130 S26 had one intermittent first-run failure at the Settings partial-import assertion after its control save and opened-original assertions passed; a separate isolated rerun reached `S26_FIRST_RUN_COMPLETE`, `S26_RESTART_RECOVERED`, `S26_ALL_ASSERTIONS_REACHED`, `PASS25`. This one-failure/one-pass result is recorded as a limit, not represented as a consistently passing release gate. The restored product/test file blobs equal `HEAD`, and `git diff --check` passes.
