# S26 real Electron lifecycle evidence — 2026-09-25

Requirement scope: `FR-DOC-019`, `FR-DOC-033`, `FR-DOC-035`, `DR-DOC-014`, `IR-MCP-018`, `IR-APP-013`. Active target `0.11.0-w2`; no draft/deprecated stability blocker. GitHub issue #49 starts from `a4b1c4285c69daff46bfc03b902f43dcc7d490f0` in isolated `DocuLightViewer-r3`. This is an uncommitted S26 result; standard R3 sourceHash is `a0f1f029c78b13269d3cb1dceea52697291f73341e842141acd143a257e196e8`.

## Product run and result

`node test/r3/run-electron.cjs --scenario s26` exits 0 with `PASS case=s26 assertions=25` and emits `S26_FIRST_RUN_COMPLETE`, `S26_RESTART_RECOVERED`, and `S26_ALL_ASSERTIONS_REACHED`. The scenario starts the actual `src/main/index.js` from the prepared Electron snapshot twice, with one OS-temp dev `userData` and document store retained across both app processes. The test inspected each owned PID and its full command line before requesting an environment-and-argv-gated graceful `app.quit()`. No broad process termination or global native rebuild was used. Run 1 PID 18700 and run 2 PID 134176 were distinct. The redacted full command line and its SHA-256 for each process are in [the S26 sample artifact](./2026-09-25-s26-electron-integration-samples.json).

The first app exposes the exact public eight-tool list through its real HTTP MCP endpoint. A public `save_document` call publishes a control Markdown file with project/category/tag metadata; the response separately reports `saved=true`, `indexing.state=queued`, and a diagnostic jobId. The ledger then records that job as completed and public keyword search finds the control. External A is opened through the product's second-instance user-open route; external B is a junction alias of the same canonical original and is opened through that same route. A real `open_markdown(documentId)` then selects the verified origin through the viewer handler without exposing the original path. The source ledger records the two lexical aliases under one document ID and one canonical hash. A distinct-canonical, same-byte file remains `duplicate_candidate` under `FR-DOC-035` AC-7; `test-opened-markdown-registrar-contract.js` confirms this. The content-only control document has no invented original aliases.

The Settings renderer/preload also invokes the real linked-import IPC with a user-selected OS-temp entry document. It imports the entry and its linked completed document, reports `imported=2` and `missing=1` for a missing third link, and keeps both completed contained files through restart. The import response contains redacted diagnostics and source-relative paths. This product sample proves partial missing-link counts and file preservation; it does not simulate a later import write fault or import cancellation. S25 separately exercises a cancelled import job and retryable retained file.

The original is rewritten from an OLD marker to a 2,782,074-byte NEW revision. The real Settings renderer and preload call `indexing:get-status` and `indexing:cancel-job` while its owner job is active. Cancel returns an owner receipt, the job becomes durably cancelled, desired revision 2 remains retryable, and both the external original and published store copy keep SHA-256 `e81eb2a05827ca2815d61ddf03fa4945d344c69b15fe0d4bc6217c73ad6896a3`. After graceful exit, the second product process starts with the same temp profile; the owner recovers and completes revision 2 in 41.19 seconds. Public NEW keyword search finds the latest snippet; the OLD query returns no result and the full unique OLD marker is absent. The same document ID, two aliases, project/category/tags, imported files, and source/store checksums remain. The recovered job states are completed, cancelled, completed. The artifact retains the seed, unique markers, document IDs, alias canonical hash, SHA-256 values, PIDs, redacted command lines, and runtime details without publishing original absolute paths.

## Test-first record

- RED command for each product assertion: `$env:DOCULIGHT_R3_ELECTRON_ROOT='<prepared Electron root>'; node test/r3/run-electron.cjs --scenario s26; exit $LASTEXITCODE`. The real product first returned exit 1 at `S26 owned product accepts graceful quit after process identity inspection`; a gated private lifecycle action now shuts down the owner and calls `app.quit()`.
- RED exit 1 at `S26 Settings cancel targets the active owner document job`: the real Settings bridge called legacy `searchEngine.cancelRebuild()` during an active owner job. `indexing:cancel-job` now targets the cached active owner job ID, while an active full rebuild retains the legacy handler.
- RED exit 1 at `S26 product keyword search retrieves saved control bytes`: after a cold missing-cache start, the owner committed keyword rows but public `search_documents` remained stale with zero indexed documents. Owner-managed `ensureFresh()` now loads only a compatible committed keyword cache through a read-only connection when SQLite `data_version` changes; missing/root/tokenizer mismatch does not trigger automatic rebuild.
- RED exit 1 at `S26 Settings import preserves two completed files and redacted partial missing count`: product linked import passed a null owner controller and failed. The real IPC handler now supplies the singleton owner, its private ingress root, and a read-only source ledger.
- RED command `node test/test-owner-managed-keyword-refresh-contract.js` in the prepared Node runtime exited 1 at `read fault must not report ready` (actual `ready`). A read fault now reports a path-free degraded diagnostic, preserves prior committed hits, and retries a compatible read. The focused test also covers cold cache creation, `data_version` refresh, source-root mismatch, and tokenizer mismatch.
- The 20-second and 60-second intermediate restart failures were fixture timing, not an owner recovery defect. The second owner reported `recoveryComplete=true` and progress 1264/1344 on a larger fixture. The final S26 lifecycle uses a bounded 2.78 MiB update, event/SQLite convergence checks, and a 60-second failure deadline. The older 10 MiB worker benchmark remains evidence for #45's separate product-route latency gate.
- Two search-oracle mistakes were removed: legacy `search_documents` echoes its query even on no-result responses, and garu-ko expands ordinary long alphanumeric queries into OR-ed bigrams. The final OLD/NEW fixture uses discriminating rare prefixes and also checks the full unique marker in result content.

## Focused regression results

The prepared Node runtime is ABI 137; the prepared Electron runtime is Electron 33.4.11, ABI 130. No shared root `node_modules` native rebuild was run. S25 Node passes 16 assertions, S23 9, S20 21, and S21 Electron 23. Unknown Electron scenario exits 2. Owner-managed keyword refresh, opened registrar distinct-canonical duplicate-candidate, Wave 2 import/adoption, SQLite keyword and metadata diagnostics, Settings indexing, Wave 2 MCP eight-tool parity, and indexed-origin-open contract tests pass. S23 retains the committed read-only keyword result when owner startup fails.

## Separate inherited gates

This proves S26's core save/open/update/cancel/restart lifecycle. #43, #45, and #46 remain OPEN. This run does not provide a complete cold product-main source/keyword writable-open audit across all Settings/import/opened/semantic routes, committed keyword checksum through failed/cancelled full rebuild plus abandoned-sidecar operator action, or the S22 product handler focus/close/status/cancel raw latency samples and thresholds. Their existing partial evidence must not be treated as a passing S26 gate or used to close those issues.
