# S23b Settings compact and clear final evidence

Scope: GitHub #61 approved simplification; `FR-DOC-019 AC-6/AC-10`, `IR-APP-010 AC-5`, `IR-APP-013 AC-15/16`, and `REL-DOC-007/008`. Code and tests are frozen at sourceHash `7c8fba9a1ed721db65373a4bf424d8e494ca10f4df4310bfaa76988a4c34be38`, based on HEAD `565a8562cc2045564519452ee57f7e4c572b115f` in the isolated R3 checkout. This report supersedes the earlier [partial S23b checkpoint](./2026-09-25-s23b-owner-clear-evidence.md) for current behavior and sourceHash. The earlier report remains a historical record.

## Behavior and semantic RED

The owner inspects read-only `PRAGMA auto_vacuum` before a compact decision. For an existing `NONE` database, it returns `started:false`, `scheduled:false`, `compacted:false`, `reason:compact-rebuild-required`, without `incremental_vacuum`, `VACUUM`, a hidden rebuild, or a keyword write. Other modes return `compacted:false`, `reason:compact-deferred`; both reasons use existing localized rebuild guidance in Settings. A semantic test first failed because compact never read `auto_vacuum`, then because mode 2 received the hardcoded `NONE` reason. Both passed after the minimal owner branch.

The Settings maintenance IPC boundary now checks the actual tracked Settings BrowserWindow. The hidden R3 Settings probe is accepted only with both the lifecycle environment and argv gates. Non-Settings rebuild, retry, cancel, compact, and clear requests do no maintenance work. A semantic test first showed an unattributed rebuild reaching the owner and a settings-like viewer URL passing the URL-only guard; both paths now reject. The existing private `rebuild_index` owner route and public eight-tool MCP contract remain separate.

## Focused SQLite and responsiveness

The [raw focused run](./2026-09-25-s23b-focused-final.txt) uses a real WAL keyword DB with a committed body-only FTS hit, `auto_vacuum=0`, 8,200 free pages, and 33,685,504 main-file bytes. Before and after deferred compact, free pages, bytes, committed generation, logical keyword SHA-256, owner query, and the `search_documents`, `search_projects`, and `smart_search` envelopes are unchanged. The compact logical SHA-256 is `8ed0bd405924ecc5f169aaf16a135929161039dd33cf68b0f17ff8fbe92f1efa` on both sides.

Confirmed owner clear validates a 33,685,504-byte online backup with `quick_check` and the old body-only hit before deleting keyword rows. The previous generation and public search remain available during backup. Across 16 samples while clear was active, owner query P95/P99/max was 1.671/1.671/1.671 ms, status P95/P99/max was 0.0249/0.0249/0.0249 ms; 263 heartbeat updates had a 45 ms largest gap. Cancel was acknowledged in 1 ms. A precommit fault and cancellation each preserved the old generation, body-only hit, and committed logical SHA-256; read-only source snapshots also preserved the source ID, root fingerprint, project/category/tags, path history, and original alias across success, fault, and cancel. The focused source hash does not cover `metadata_json`; the real S26 successful-clear oracle compares that field. Restart resolves interrupted and postcommit-finalization cases. The owner leaves Markdown and an unrelated queued job intact. These fixture timings are samples, not population latency percentiles.

The focused artifact SHA-256 is `a82ca66ad3fe6f43ab5ec2a815ac5f64f835a22c43ec56c6b8a8d5efb38c0828`. The checksums describe selected committed logical rows and hits, not the raw DB/WAL/SHM bytes; WAL and checkpoint timing make a raw file hash an unsuitable preservation oracle here.

## Real Electron Settings and writer audit

The [exact-hash S26 raw result](./2026-09-25-s23b-s26-electron-final.json) passed 40 assertions across two inspected cold Electron PIDs. Its real Settings renderer/preload compact call returned the `NONE` fallback while the logical keyword SHA-256 and source aliases remained unchanged. Declining the native clear confirmation left committed search intact. Confirming clear created a durable owner job that reached `completed`, removed the committed keyword generation, and made the public hit disappear. Its readable backup has the same logical SHA-256 and hit as the old generation. The post-clear source rows, including `source_id`, revision, project, category, tags, and metadata, match before clear; all four aliases and the IDs/statuses of seven preexisting jobs match. The before/backup logical SHA-256 is `90fb098eeee03cf827abc3f83aa548a3f1df1c0b5f7f1ff595f8e26e49149d1f`; the cleared keyword state is `2f0339802623cd67317fde3a345d9540fa182f4d96824b99797fe72984f929c8`. The raw result SHA-256 is `516c2aa694f1070f903254f369aa4356df4f635e32953b026ad5c4da0857d299`.

The same S26 run loaded the [SQLite constructor audit](./2026-09-25-s23b-sqlite-open-redacted.jsonl) before product startup. Its [summary](./2026-09-25-s23b-sqlite-open-summary.json) records 61 product opens: four writable opens in the long owner (keyword and source DB in each run), zero writable opens in main or short worker, 57 read-only product opens, and 34 read-only auxiliary opens. The original local audit SHA-256 is `2a01834829fd50f3919d873a510ac9d9a350790ef17ff946a5a7cfac92eca23d`; the checked-in redacted JSONL SHA-256 is `8e13eab91b5c334f05ab52bde86922acfb978e987d0174866775540cb84c3a3e`. The earlier #62 audit covers a different sourceHash and is supporting history only.

One intermediate S26 run at sourceHash `0197152d607550183fdafbb3d117f113c71dc47ededf41d77224a41c90c20da5` timed out on its 3-second private IPC deadline before the maintenance assertions. Its [failed raw result](./2026-09-25-s23b-s26-electron-final-failed.json) is retained. The exact final-hash run passed without that timeout.

## Gates

`node test/test-s23b-maintenance.js`, `node test/test-settings-indexing-contract.js`, and `node test/test-sqlite-keyword-index-contract.js` passed. Final-hash R3 results: S23 45, S20 21, S19 47, S17 32, and real Electron S26 40 assertions. Two independent subagent axes reviewed the source/boundary and product/evidence coverage; their findings on sender identity and source metadata/job evidence were incorporated, with no remaining Critical/High/Medium finding after re-review. No commit, push, GitHub issue mutation, or source requirement-status change is part of this evidence freeze.
