# S03 Node/Electron focused harness evidence

Issue: #26. Start SHA: `7db991d5c76259435c80a8ce1ed2d925fcbd1e78`. Related SRS: `FR-DOC-019` (stable), `DR-DOC-014`, `REL-DOC-009`, `IR-APP-013` (evolving). S03 supplies focused verification infrastructure for these requirements; it does not claim their product acceptance criteria are complete.

## Test first

1. `node --test test/r3/test-harness-contract.cjs` initially exited 1 with four `AssertionError`s for missing/unknown selection, duplicate registration, setup/assertion distinction, and positive PASS count. This was against an inert dispatcher placeholder; no module, fixture, or ABI error was counted as RED.
2. Fixture and manifest preflight were separately written against inert placeholders. Their recorded RED included `AssertionError: fixture must be created` and `AssertionError: undefined !== false`. The contract then reached 6/6 GREEN.
3. CLI usage, prepare argument validation, cleanup failure, Electron app environment, source-only snapshot reuse, and exclusion of unrelated untracked files each produced a focused `AssertionError` RED before its implementation. The original console output was observed in the author tool session and reported to the root agent during implementation; no separate preimplementation transcript artifact was written. The source hashes below are final runtime snapshot hashes, not claimed preimplementation hashes.
4. Independent review found four HIGH defects. A later `node --test test/r3/test-harness-contract.cjs` exited 1 with four concrete `AssertionError`s before fixes: untracked product source absent (`false !== true`), zero/missing Electron PASS accepted (`true !== false`), junction outside OS-temp accepted (`true !== false`), and case dependency from isolated root unresolved (`undefined !== 'isolated'`). A fifth focused RED (`1 !== 2`) reproduced a child module error misclassified as an assertion. A later real live Node child case timed out in 150 ms; the pre-fix contract failed `0 !== 2`, and the final bounded child returned `SETUP_ERROR`/exit 2. The untracked probe was changed to unique `wx` files so it cannot overwrite a pre-existing user file. Final focused contract command exited 0, 17/17, 1234 ms.
5. `node test/r3/run-node.cjs --case s04` exited nonzero and printed `SETUP_ERROR ... available: harness-self`; `s04` remains unregistered for the next issue.

## Isolated runtime preparation and focused GREEN

`node test/r3/prepare-runtime.cjs --node-root %TEMP%/doculight-r3-node-20260924d --electron-root %TEMP%/doculight-r3-electron-20260924d` created separate full source snapshots with final SHA-256 `2f8ee4ff8cd134898c0ddc9cd2cac03d38ecc5187cbb3ab2394b5ae299a00eef`. It ran `npm ci --ignore-scripts` in each root, `npm rebuild better-sqlite3` in Node, and `node scripts/rebuild-electron-native.js` in Electron. Both real SQLite probes passed, with Node ABI 137 and Electron ABI 130. The installed Electron package initially lacked its executable because the install script exited 0 under `ELECTRON_SKIP_BINARY_DOWNLOAD`; the preparation code checked `path.txt` and restored the matching 33.4.11 executable from the locally verified package cache. It did not treat the installer exit 0 as proof of a usable executable.

After a source-only harness edit, the same roots were refreshed in 1.1 seconds without `npm ci` or native rebuild. The Node native module modification time was unchanged. The final refresh plus both focused cases took 2.3 seconds, again with unchanged native mtime. The final snapshot hash is `2f8ee4ff8cd134898c0ddc9cd2cac03d38ecc5187cbb3ab2394b5ae299a00eef`; nonignored untracked product files under `src`, `scripts`, `test`, `assets`, and `build` are included while unrelated untracked root files and analysis/plan evidence are excluded. Existing symlink/junction targets outside OS-temp are rejected before copying or installing. Dependency/runtime changes require fresh empty OS-temp roots.

| Command | Result |
|---|---|
| `node test/r3/run-node.cjs --case harness-self` with `DOCULIGHT_R3_NODE_ROOT` set to the Node root | exit 0, `PASS case=harness-self assertions=3` |
| `node test/r3/run-electron.cjs --scenario harness-self` with `DOCULIGHT_R3_ELECTRON_ROOT` set to the Electron root | exit 0, `PASS case=harness-self assertions=3` after Electron `app.whenReady()` |

Environment: Windows 10.0.26200 x64; Node v24.16.0; Electron 33.4.11. The Node launcher runs each case in a bounded 60-second child; a missing terminal PASS or timeout is a setup failure, while a matching assertion marker is an assertion failure. Each selected case loads from its isolated source snapshot and opens the snapshot's real `better-sqlite3` module in addition to querying the real temporary DB fixture and checking Markdown bytes. DB handles are closed and only owned OS-temp fixture directories are removed. The Electron child closes any windows before exit; its launcher accepts exit 0 only with the exact selected scenario's positive terminal PASS marker. Source/runtime manifest mismatch returns `SETUP_ERROR` and prints the exact preparation command. Contract tests distinguish `ASSERTION_FAIL` (exit 1) from `SETUP_ERROR` (exit 2). The independent [TDD review](2026-09-24-s03-tdd-review.json) and [native isolation review](2026-09-24-s03-native-review.json) both pass with no remaining finding. The original pre-implementation RED transcript and source snapshot were not preserved, so their exact chronology cannot be reconstructed from this final tree; the recorded commands/assertions and later focused REDs remain the available evidence.
