# Phase 4 — Lazy Startup Recovery Queue

## Requirements

`FR-DOC-019`, `REL-DOC-007` AC-2/AC-4, `SEC-DOC-003`.

## Files

- Create `test/test-startup-index-recovery-memory-contract.js`.
- Modify `src/main/search-engine.js`.
- Modify `package.json`.
- Update `verification/phase-4-verification.md`.

## TDD Tasks

- [x] RED: 대형 UTF-8 문서 여러 개의 recoverable jobs를 reconcile한 뒤 `_smartIndexQueue` item이 `content` 문자열을 보유하지 않는지 실행형 contract를 추가한다.
- [x] RED: content hash mismatch 기존 결과를 유지하고 lifecycle/ledger regression으로 missing/outside-root/fresh-heartbeat를 보존한다.
- [x] RED: queue가 verified `contentHash`와 known document identity/jobId를 보존하고 worker가 처리 시 파일을 lazy read할 수 있음을 검증한다.
- [x] GREEN: `queueKnownDocumentIndex`가 preverified content hash를 받고 queue content를 null로 유지할 수 있게 한다.
- [x] GREEN: async/sync startup reconciliation이 `ledger.getDocument(job.documentId)`와 preverified hash를 사용해 known-document queue로 resume한다.
- [x] GREEN: 각 문서 검증 후 queue에 content reference를 남기지 않고 기존 event loop yield를 유지한다.
- [x] GREEN: 신규 hotfix tests를 표준 `test:wave2` script에 편입한다.

## Acceptance

- queued payload memory가 document body 총합에 비례하지 않는다.
- worker는 content가 null이면 기존처럼 UTF-8 파일을 처리 직전에 읽는다.
- startup recovery status/diagnostic과 jobId reuse 의미는 유지한다.
- `test:wave2` 한 번으로 aggregate/poller/coalescer/recovery 회귀가 모두 실행된다.

## Commands

```powershell
node test/test-startup-index-recovery-memory-contract.js
npm run test:wave2
```
