# Phase 3 — Bounded Worker Heartbeat Writes

## Requirements

`REL-DOC-007` AC-2/AC-4, `REL-DOC-008` AC-2.

## Files

- Modify `test/test-search-index-worker-contract.js`.
- Create `test/test-status-coalescer-contract.js`.
- Create `src/main/status-coalescer.js`.
- Modify `src/main/search-index-worker.js`.
- Update `verification/phase-3-verification.md`.

## TDD Tasks

- [x] RED: fake clock으로 1,000개 burst status가 500 ms window당 한 번만 sink에 기록되고 latest status가 보존되는 실행형 contract를 추가한다.
- [x] RED: `flush()`가 pending status를 즉시 내보내며 `cancel()`이 pending timer를 제거하는지 검증한다.
- [x] RED: worker의 timer와 `onRebuildStatus`가 모두 동일 coalescer를 통과하고 50 ms literal이 없음을 요구한다.
- [x] GREEN: reusable latest-status coalescer를 구현한다.
- [x] GREEN: monitor cadence를 500 ms로 바꾸고 모든 worker nonterminal status를 coalescer로 보낸다.
- [x] GREEN: completion/cancel/failure 직전에 pending status를 flush하고 terminal message는 즉시 전송한다.
- [x] REFACTOR: controller terminal patch와 cancellation checkpoint 회귀를 유지한다.

## Acceptance

- timer/per-file event를 합친 nonterminal durable update 최대 빈도는 초당 2회/job이다.
- rebuild live UI는 500 ms cadence를 유지한다.
- terminal patch, cancellation checkpoint, packaged worker boundary는 변화하지 않는다.

## Commands

```powershell
node test/test-search-index-worker-contract.js
node test/test-status-coalescer-contract.js
node test/test-search-index-worker-benchmark-contract.js
```
